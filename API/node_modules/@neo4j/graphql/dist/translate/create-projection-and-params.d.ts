import { ResolveTree } from "graphql-parse-resolve-info";
import { Node } from "../classes";
import { Context } from "../types";
interface ProjectionMeta {
    authValidateStrs?: string[];
    connectionFields?: ResolveTree[];
    interfaceFields?: ResolveTree[];
    rootConnectionCypherSortFields?: {
        alias: string;
        apocStr: string;
    }[];
}
declare function createProjectionAndParams({ resolveTree, node, context, chainStr, varName, literalElements, resolveType, inRelationshipProjection, isRootConnectionField, }: {
    resolveTree: ResolveTree;
    node: Node;
    context: Context;
    chainStr?: string;
    varName: string;
    literalElements?: boolean;
    resolveType?: boolean;
    inRelationshipProjection?: boolean;
    isRootConnectionField?: boolean;
}): [string, any, ProjectionMeta?];
export default createProjectionAndParams;
//# sourceMappingURL=create-projection-and-params.d.ts.map